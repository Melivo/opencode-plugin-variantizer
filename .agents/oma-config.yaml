# Migrated by oma migration 008 — model_preset single-file config
mcp:
  devtools_browsers:
    - chrome
vendors:
  - codex
  - opencode
providers:
  web: native
  code_intelligence: gortex
  semantic_memory: honcho
honcho:
  base_url: http://127.0.0.1:28100
  workspace_id: visimeos
  recall_mode: messages
model_preset: antigravity
